{
  "name": "@elastic/ecs",
  "version": "1.0.0-alpha.1",
  "private": true,
  "main": "generated/index.ts",
  "bin": {
    "generate-ecs-types": "./bin/generate-ecs-types.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "rm -rf build && tsc --watch",
    "generate-ecs-types": "node bin/generate-ecs-types.js && prettier --write . ",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/elastic/ecs-typescript.git"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^17.0.11",
    "commander": "^8.3.0",
    "jest": "^27.4.7",
    "js-yaml": "^4.1.0",
    "prettier": "2.5.1",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  }
}
